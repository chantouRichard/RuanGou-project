<UserControl x:Class="frontend.Views.Pages.MemoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:frontend.Models"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- 转换器定义 -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style TargetType="TextBlock" x:Key="TaskTitleStyle">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                    <Setter Property="Foreground" Value="Gray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 添加任务区域 -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="任务标题：" FontSize="12" VerticalAlignment="Center"></TextBlock>
                <TextBox x:Name="TaskTitleTextBox" Width="200" Height="30" 
                         VerticalContentAlignment="Center" Margin="10,0,10,0"
                         TextWrapping="NoWrap" ToolTip="输入任务标题"/>

                <TextBlock Text="任务描述：" FontSize="12" VerticalAlignment="Center"></TextBlock>
                <TextBox x:Name="TaskDesciptionTextBox" Width="200" Height="30"
                         VerticalAlignment="Center" Margin="10,0,10,0"
                         TextWrapping="NoWrap" ToolTip="输入任务描述"/>

                <DatePicker x:Name="DueDatePicker" Width="120" Height="30" 
                            SelectedDate="{x:Static sys:DateTime.Now}" Margin="0,0,10,0"/>

                <Button Content="添加任务" Width="80" Height="30" 
                        Click="AddTask_Click" Background="#4CAF50" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- 操作按钮区域 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="刷新列表" Width="80" Height="30" Margin="0,0,10,0"
                    Click="RefreshList_Click" Background="#2196F3" Foreground="White"/>

            <Button Content="清除已完成" Width="100" Height="30"
                    Click="ClearCompleted_Click" Background="#F44336" Foreground="White"/>
        </StackPanel>

        <!-- 任务列表 - 简化GridView版本 -->
        <ListView Grid.Row="2" x:Name="TodoListView" Margin="10"
          ScrollViewer.HorizontalScrollBarVisibility="Auto"
          Background="Transparent">
            <ListView.View>
                <GridView>
                    <!-- 复选框列 -->
                    <GridViewColumn Header="完成" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsCompleted}" 
                                  HorizontalAlignment="Center"
                                  Checked="TaskCompleted_Changed" 
                                  Unchecked="TaskCompleted_Changed"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- 标题列 -->
                    <GridViewColumn Header="任务标题" Width="200" DisplayMemberBinding="{Binding Title}"/>

                    <!-- 截止日期列 -->
                    <GridViewColumn Header="截止日期" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- 描述列 -->
                    <GridViewColumn Header="任务描述" Width="250" DisplayMemberBinding="{Binding Description}"/>

                    <!-- 操作按钮列 -->
                    <GridViewColumn Header="操作" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="编辑" Width="50" Margin="2" 
                                    Click="EditTask_Click" Tag="{Binding Id}"
                                    Background="#FFC107" Foreground="White"/>
                                    <Button Content="删除" Width="50" Margin="2"
                                    Click="DeleteTask_Click" Tag="{Binding Id}"
                                    Background="#F44336" Foreground="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- 编辑窗口 -->
        <Popup x:Name="EditTaskPopup" 
               Placement="Center" 
               StaysOpen="False"
               Width="400" 
               Height="360">
            <Border Background="White" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="1"
                    CornerRadius="10">
                <StackPanel Margin="10">
                    <TextBlock Text="标题:" Margin="0,0,0,5"/>
                    <TextBox x:Name="EditTitleTextBox" Margin="0,0,0,10"/>

                    <TextBlock Text="截止日期:" Margin="0,0,0,5"/>
                    <DatePicker x:Name="EditDueDatePicker" Margin="0,0,0,10"/>

                    <TextBlock Text="描述:" Margin="0,0,0,5"/>
                    <TextBox x:Name="EditDescriptionTextBox" AcceptsReturn="True" 
                             TextWrapping="Wrap" Height="80" Margin="0,0,0,10"/>

                    <CheckBox x:Name="EditIsCompletedCheckBox" Content="已完成" Margin="0,10,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="保存" Width="80" Margin="0,0,10,0" 
                                Click="SaveTask_Click" Background="#4CAF50" Foreground="White"/>
                        <Button Content="取消" Width="80" Click="CancelEdit_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>