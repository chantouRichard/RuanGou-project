<Page x:Class="frontend.Views.Pages.AIPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:frontend.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="AIPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock FontSize="28" FontWeight="Bold" Text="AI工具页面" Margin="0,0,0,20"/>

            <!-- 翻译功能区 -->
            <GroupBox Header="文本翻译" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox x:Name="TranslateInputTextBox" Width="400" Height="32" Margin="0,0,10,0" VerticalAlignment="Center" Text="请输入要翻译的文本"/>
                        <ComboBox x:Name="LanguageComboBox" Width="120" Height="32" VerticalAlignment="Center">
                            <ComboBoxItem Content="英文" Tag="en"/>
                            <ComboBoxItem Content="中文" Tag="zh"/>
                            <ComboBoxItem Content="日文" Tag="jp"/>
                            <!-- 其他语言可自行添加 -->
                        </ComboBox>
                        <Button x:Name="TranslateButton" Content="翻译" Width="80" Height="32" Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="TranslateResultTextBlock" Text="翻译结果将在此显示" FontStyle="Italic" Foreground="Gray"/>
                </StackPanel>
            </GroupBox>

            <!-- 文本总结功能区 -->
            <GroupBox Header="文本摘要" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <TextBox x:Name="SummaryInputTextBox" Height="80" AcceptsReturn="True" TextWrapping="Wrap" Text="请输入需要总结的长文本"/>
                    <Button x:Name="SummaryButton" Content="生成摘要" Width="100" Height="32" Margin="0,10,0,0"/>
                    <TextBlock x:Name="SummaryResultTextBlock" Text="摘要结果将在此显示" FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap"/>
                </StackPanel>
            </GroupBox>

            <!-- 图片识别功能区 -->
            <GroupBox Header="图片识别" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="SelectImageButton" Content="选择图片" Width="100" Height="32"/>
                        <Image x:Name="SelectedImagePreview" Width="100" Height="100" Margin="10,0,0,0"/>
                        <Button x:Name="ImageRecognizeButton" Content="识别图片" Width="100" Height="32" Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="ImageRecognitionResultTextBlock" Text="图片识别结果将在此显示" FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap" Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</Page>
